services:
  client:
    container_name: solace-cms-client
    build:
      context: ./client
      args:
        ENV: development
    volumes:
      - ./client/src:/client/src
      - ./client/.svelte-kit:/client/.svelte-kit
    ports:
      - 3000:3000

  directus:
    container_name: solace-cms-directus
    image: directus/directus:10.8.3
    ports:
      - 8055:8055
    volumes:
      - ./directus/database:/directus/database
      - ./directus/uploads:/directus/uploads
      - ./directus/extensions:/directus/extensions
    environment:
      KEY: "jsklrhqikrgbqhujfgqlrqgyuieroqyiureqi"
      SECRET: "sqhlasjfghassdguqwgfbjhqvrjhqwerfqwjevgqwjegqwje"
      ADMIN_EMAIL: "admin@solace.digital"
      ADMIN_PASSWORD: "S1l2c3"
      DB_CLIENT: "sqlite3"
      DB_FILENAME: "/directus/database/data.db"
      WEBSOCKETS_ENABLED: true
        # CACHE_ENABLED: "true"
        # CACHE_STORE: "redis"
        # REDIS: "redis://cache:6379"
